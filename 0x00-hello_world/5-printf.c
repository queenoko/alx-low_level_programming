#include <stdio.h>
/**
 * main - Program that prints exactly the proper grammer function
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}

